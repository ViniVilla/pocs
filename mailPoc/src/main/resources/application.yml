server:
  port: 8081

spring:
  mail:
    host: localhost
    port: 1025
    username: ${FROM_EMAIL}
    password: ${FROM_PASSWORD}
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true