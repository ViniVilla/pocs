server:
  port: 8081

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${FROM_EMAIL}
    password: ${FROM_PASSWORD}
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true